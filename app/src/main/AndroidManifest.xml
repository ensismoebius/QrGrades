<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QrGrades"
        tools:targetApi="31">
        <activity
            android:name=".activities.ListStudents"
            android:exported="false" />
        <activity
            android:name=".activities.AddOrEditStudent"
            android:exported="false" />
        <activity
            android:name=".activities.DisciplineList"
            android:exported="false" />
        <activity
            android:name=".activities.DisciplineDetail"
            android:exported="false" />
        <activity
            android:name=".activities.ClassGroupList"
            android:exported="false" />
        <activity
            android:name=".activities.GoalList"
            android:exported="false" />
        <activity
            android:name=".activities.StudentProgress"
            android:exported="false" />
        <activity
            android:name=".activities.BackupList"
            android:exported="false" />
        <activity
            android:name=".activities.WebServerActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".activities.Main"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            tools:targetApi="27">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.QrScanTileService"
            android:exported="true"
            android:icon="@drawable/ic_qr_scan_tile"
            android:label="@string/qr_scan_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".services.WebServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:targetApi="34">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local LAN web admin server for teacher gradebook access" />
        </service>
    </application>

</manifest>